CREATE TABLE UserType (
    id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    description TEXT,
    profileDetails TEXT
);

insertUserType:
INSERT INTO UserType(description, profileDetails)
VALUES (?, ?);

getAllUserTypes:
SELECT * FROM UserType;

getUserTypeById:
SELECT * FROM UserType
WHERE id = ?;

deleteUserTypeById:
DELETE FROM UserType
WHERE id = ?;