CREATE TABLE Language (
    id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    name TEXT,
    fluencyLevel TEXT
);

insertLanguage:
INSERT INTO Language(name, fluencyLevel)
VALUES (?, ?);

getAllLanguages:
SELECT * FROM Language;

getLanguageById:
SELECT * FROM Language
WHERE id = ?;

deleteLanguageById:
DELETE FROM Language
WHERE id = ?;